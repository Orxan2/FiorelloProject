@model IEnumerable<ProductCategory>
@{
    ViewData["Title"] = "Index";
}

<!-- PRODUCTS START -->

<section id="products">
    <div class="container">      
        <div class="row" id="addHere">
            <partial name="_LoadMore"></partial>           
        </div>
        <div class="row">
            <input type="hidden" value="@ViewBag.ProductCount" id="ProductCount"/>
            <button class="btn btn-primary text-white fw-bold col-12" id="load">Load More...</button>
        </div>

    </div>
</section>

<!-- PRODUCTS END -->
